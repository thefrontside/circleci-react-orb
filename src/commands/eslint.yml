description: >
  Lint JS

steps:
  - run:
      name: Ensure artifacts directory is present
      command: mkdir -p artifacts/eslint

  - run: yarn eslint
      --max-warnings=0
      --format junit
      --output-file ./artifacts/eslint/eslint.xml
